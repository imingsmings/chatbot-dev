# UI Design QA

日期：2026-09-07。下方先保留桌面轮历史记录，最新移动版视觉与验证记录位于文末。桌面轮的 19 项完整 Mock 结果不代表移动版执行范围。

## 比较基准

- 源图：`proto/ui-refresh-2026-09-07/focus-dark.png`、`reading-light.png`、`workspace-context.png`。
- 实拍：同目录 `implemented/reference-desktop-dark.png`、`reference-model-light.png`、`reference-context-dark.png`。
- 三组源图和实拍均为 1487x1058 像素，实拍 CSS 视口 1487x1058、deviceScaleFactor=1，无浏览器工具栏，不做缩放或像素差百分比推断。
- 每次比较将源图与相应实拍放入同一输入，进行了整页构图对照和模型菜单、输入区、表格、身份区、上下文统计的局部检查；这些区域在原尺寸下可读，无需额外缩放裁剪。
- 暗色为消息与草稿状态，浅色打开模型菜单，上下文为概览打开状态。模型选项、思考档位、会话日期分组和预览数字为现有 Mock 返回，和源图示例不完全相同，不将动态文案差异算作视觉缺陷。
- 源图是生成式视觉方向，不是像素规范。经确认的正文 14px、248px 侧栏、820px 阅读列、320px 上下文栏、无边框加号和无假功能规范优先，因此实拍密度更紧凑、留白更多。

## 必检项目

| 项目 | 结论与证据 |
| --- | --- |
| 字体排版 | 使用既有 Inter/system-ui 与系统中文回退；正文 14px/24px、内容标题 16/18px、菜单 14px、分组 12px、字间距 0；不依据生成图放大字体 |
| 布局间距 | 1440x1000 下消息与输入区 x=434、宽=820；输入区高=104、底部=976；浅深色矩形完全一致；侧栏 248，上下文 320 |
| 色彩 | 深色 #181818/#141414/#262626，浅色白色与中性浅灰；浅色用户消息黑底白字；绿色仅用于选中与预算强调；可用模型和思考选项同色 |
| 资产图标 | 保留项目已有 `/assets/jw.svg` 头像；Lucide 统一图标；未新增虚构在线圆点、点赞、语音、装饰图或替代品牌图 |
| 文案 | 操作统一中文，模型名和技术标识保留原文；原始消息与工具数据折叠；禁用模型附不可用/未配置说明 |
| 交互 | 单层菜单、键盘方向键与 Enter、Escape、唯一勾选、焦点恢复、会话切换清草稿、上下文快照过期清理有自动断言 |
| 移动端 | 390/320px 抽屉、52px 顶栏、104px 输入区，无页面横向溢出；仅表格横向滚动，正文位置不随之改变；390x500 多行输入限高 96px |
| Tooltip | 新建、上下文关闭、参数弹窗关闭的提示标签位于提示容器内，提示位于视口内、无 SVG 箭头；浏览器 Runtime 异常列表为空 |

## 比较与修正历史

1. 第一轮发现 Markdown 列表标记被基础样式重置，丢失有序步骤层级（P2）；补充局部 ul/ol list-style，最终暗色图步骤 1/2/3 清晰，DOM 断言为 decimal。
2. 第一轮用户消息与回答间距过大（P2）；统一行间距并缩短用户行底部留白，最终主题对照保持一致。
3. 侧栏浏览器断言发现异步预览按钮临时禁用后丢失返回焦点（P2）；提供显式返回按钮 ref，桌面及移动端 Escape 后均断言焦点返回上下文按钮。
4. 手机第一轮默认模型名被“思考 · 高”挤压、表格沿整个回答横向滚动（P2）；手机只显示档位，表格改为独立滚动容器。`implemented/mobile-390.png` 可完整显示 DeepSeek V4 Flash，自动断言最后一列可滚入且正文位置不变。
5. 最后一轮以修改后的同视口/主题/数据重新捕获并比较；没有发现剩余 P0/P1/P2 视觉问题。截图不是功能测试的替代，功能结论仍以测试结果为准。

## 验证记录

- `bun run check`：类型检查及标准/类型感知 lint 通过。
- `bun run test:client`：最终复跑 130/130，通过 28 个测试文件。
- `bun run build`：最终复跑通过，包含服务端/客户端类型检查、标准及类型感知 lint、客户端生产构建。
- `DEBUG_PORT=9425 CDP_SCREENSHOTS=1 bun tests/cdp/scenarios/ui/workspace-layout.mjs`：通过，包括三组对照图、移动布局、表格、菜单键盘、Tooltip、抽屉焦点与短视口断言。
- `CDP_SCRIPT_RETRIES=0 bun tests/cdp/run-cdp-regression.mjs all-mock`：最终 19/19 脚本全部通过，逐脚本 attempts=1、exitCode=0。记录见 `proto/ui-refresh-2026-09-07/implemented/all-mock.json`。早期运行暴露旧界面选择器、抽屉入口及长回复预条件不再成立，已按同等语义更新；附件测试等待响应式状态切换后测量，保留页面边界断言，未启用自动重试。
- 18 个修改/新增的 CDP 脚本通过 `node --check`；`git diff --check` 通过。
- 未调用真实模型、付费 Provider 或 Docker。真实 Provider/Docker 脚本仅更新定位，未运行。

## 剩余边界

- 手机检查来自 Chrome CDP 仿真，390x500 只代表短视口，不声称验证 iOS/Android 真机软键盘与系统缩放。
- `proto/` 被 Git 忽略，实际图片本地交付；可提交的说明和测试留在源码目录。
- P3：模型名极长时保持省略和完整可访问名称；当前五个模型无需引入额外搜索功能。

## 实施清单

- [x] 三张方向图对应的 UI 完整实现。
- [x] 空态、移动抽屉与过期上下文行为落地。
- [x] 同尺寸视觉对照、局部检查和问题复拍完成。
- [x] 最终静态、单测、构建与 19 项完整 Mock 门禁完成。

桌面轮结果：passed。

测试脚本创建的浏览器 profile 与临时服务由 finally 清理；仅保留请求启动的 5173 前端和 7002 隔离预览后端。未修改原有会话，未提交代码。

## 移动版比较基准

- 源图：`proto/mobile-ui-redesign-2026-09-07/chatgpt-inspired-dark.png`，853x1844px。
- 实现：同目录 `implemented/chat-dark-390.png`、`chat-light-390.png`，Chrome CDP，CSS 视口 390x844，deviceScaleFactor=1，输出 390x844px。
- 同输入对照：`implemented/comparison.html` 由浏览器渲染为 `comparison.png`。源图按原比例显示为 390x843.094px，两张实拍保持 390x844px；不拉伸、不增加设备边框、不进行像素差百分比推断。1px 高差是生成图比例误差。
- 状态：四条 Mock 历史消息、折叠思考、当前模型、草稿“用北京举例”；浅深色共用同一数据和视口。源图中的“DeepSeek”是构图示例，实现显示真实目录的具体模型。
- 对照已实际打开并在同一输入检查整体构图、顶部、正文、操作和输入区。390px 的原始实拍文字和图标可读，无需额外放大；模型面板、短视口、附件和空态另以原尺寸截图检查。

## 移动版必检表

| 保真面 | 结论与证据 |
| --- | --- |
| 字体排版 | 保留既有 Inter/system-ui 与中文系统回退、字距 0；正文 14px/24px，消息标题 16px，分组 12px、模型项 14px。输入普通/聚焦都是 16px/24px，320px 不按视口缩字；长模型名保留完整可访问名称 |
| 间距布局 | 顶部 52px、正文左右 16px、底部输入 52px 起，外侧 12px、圆角 26px；发送视觉 32px、热区 44px。浅深色关键矩形一致，320/375/390/430 无整页横向溢出 |
| 颜色变量 | 暗色聊天 #212121、输入/用户气泡 #303030；浅色白底、输入 #f1f1f1、用户消息黑底白字。可选模型与思考选项同色；禁用项降低对比并附状态，分组通过字号与间距区别 |
| 图像与图标 | 纯聊天界面无装饰插画；保留 Lucide 图标与原有侧栏头像资产，不新增手绘替代图或假语音。源图纹理按已确认规范移除。附件截图中的白色图像为测试用 1px PNG fixture，不是资源加载失败 |
| 文案内容 | 技术消息含义与结构保留；不从生成图硬编码模型名/能力，不编造思考内容。保留历史编辑、最后一条回答的复制/重生成，因此工具数量与概念图略有不同 |

### 已解决的问题

1. [P2] 用户消息工具占据额外一行，扩大问答间空白。手机编辑入口改到气泡左侧，44px 热区独立，不遮盖正文；最终双主题实拍确认。
2. [P2] 思考档位在 390px 下出现单独末行，分布不均。改为等宽弹性排列，最低 44px，选中以背景/边界/字重区分；`model-sheet-light.png`、`model-sheet-dark.png` 复拍无孤立选项。
3. [P2] 旧 `.composer-input:focus` 优先级覆盖手机字号与高度上限。手机普通/聚焦/键盘聚焦使用一致规则；CDP 实际触摸聚焦后断言 16px、常规上限 144px、短视口上限 96px，`short-viewport-draft.png` 复拍确认。
4. 早期截图包含 macOS 合成器滚动回弹，并非 DOM 留白；截图前等待滚动稳定且断言首条消息 top < 100px。最终 `comparison.png` 中不再存在异常顶部空白。
5. 新测试最初使用了不稳定的会话序号、相邻毫秒时间和过早的焦点断言。改为明确非当前会话、固定 fixture 更新时间、document 换代与异步焦点等待；本地 Base UI 源码确认焦点守卫通过 requestAnimationFrame 转移。保留草稿清理、焦点留在面板、方向键更改和唯一选中断言，未删除关键测试。

### 接受的差异

- 生成图不是逐像素规范：用户气泡根据真实文本取宽，不为了模仿图片强制短句换行；不复制纹理和模糊字形。
- 保留原有编辑和最后一条回答工具，可触达性优先于概念图省略的操作；没有新增业务能力。
- 浅色与模型底部面板属于同一设计规范的补全，原暗色概念图未展示这些状态；通过实际截图与自动断言验收，不声称它们是源图逐像素复刻。
- P3：特别长的模型名在 320px 顶栏省略，可通过底部面板查看完整名称；不引入新的搜索或别名配置。

### 移动版验证与边界

- `bun run test:client`：30 文件、137/137 通过。
- `bun run build`：通过，包含服务端/客户端类型检查、标准及类型感知 lint 和生产构建。
- `mobile-chat-layout.mjs`：聚焦专项通过；覆盖双主题四种宽度、触摸/Tab/方向键、保存等待/失败回滚/刷新、草稿切换、模板、短视口、空态、停止与恢复、目录缺失及返回桌面；页面 Runtime 异常列表为空。
- `image-attachments.mjs`：专项通过，新增手机上传失败/重试、44px 热区、多个附件内部滚动、移除后 52px 恢复断言，保留原有图片/分支/停止流程。
- 最终 `CDP_SCRIPT_RETRIES=0 DEBUG_PORT=9427 CDP_SCREENSHOTS=1 bun tests/cdp/run-cdp-regression.mjs ui`：9/9，通过；每个脚本 attempts=1、exitCode=0、timedOut=false。运行时间 2026-09-07 13:42:59 至 13:45:32（Asia/Shanghai），结果随 `implemented/ui.json` 归档。此前焦点断言失败的集合已修正后完整复跑，不把最终零重试描述为开发过程从未失败。
- 五个本轮变更的 CDP 文件通过 Node `--check`，`git diff --check` 通过。未使用 Bun `--check` 作为语法校验证据。
- 没有执行本轮全量 20 项 Mock、真实 Provider、Docker 或真机软键盘。视觉视口 Hook 单测与 Chrome 短视口只证明模拟条件，iOS/Android 真机、系统缩放、系统文件选择器仍需独立验证。

### 后续全端验证

用户确认后的全量 Mock、真实 Provider、移动主链路与平板断点验收见 [全端回归报告](docs/full-stack-regression-2026-09-07.md)。该报告保留首轮真实失败和复测记录，不能把单次复测成功当作服务稳定性保证；仍未运行 Docker 或真机测试。

移动版最后同输入比较未发现剩余 P0/P1/P2 视觉问题。源码、样式与测试保留在项目内；原有桌面改动与会话数据未回滚。图片位于被 Git 忽略的 `proto/`，未提交代码。

- [x] 顶栏、底部输入与模型面板实现。
- [x] 浅深色与窄屏/短视口复拍。
- [x] 五个保真面检查，早期问题修正后再对照。
- [x] 焦点与输入聚焦的关键断言落实到文件。
- [x] 最终 9 项 UI 集合、附件专项、137 项组件单测与生产构建通过；测试浏览器清理。

final result: passed
